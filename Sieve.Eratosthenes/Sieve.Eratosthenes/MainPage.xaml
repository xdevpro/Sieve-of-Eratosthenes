<Page
    x:Class="Sieve.Eratosthenes.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sieve.Eratosthenes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="DarkGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button HorizontalAlignment="Stretch" Content="Start" Height="50" Command="{Binding StartCommand}"></Button>
            <Button HorizontalAlignment="Stretch" Content="Stop" Height="50" Command="{Binding StopCommand}"></Button>
        </StackPanel>
        
        <ListView Grid.Row="1" ItemsSource="{Binding PrimeNumbers, Mode=TwoWay}"></ListView>
    </Grid>
</Page>
